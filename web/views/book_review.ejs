<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Document</title>
</head>
<body>
    <div>
        <img src="<%= (data.book||{}).imUrl %>" alt="">
        <h1><%= (data.book||{}).asin %></h1>
        <p>Description: <%= ((data.book||{}).description|| "-") %></p>
        <p>price: $<%= (data.book||{}).price %></p>
    </div>
    <div>
        Related:
            <% (((data.book||{}).related||{}).also_bought||[]).forEach(function(name) { %>
                <li><a href="/book/<%= name %>"><%= name %></a></li>
              <% }); %>
    </div>
    Reviews:
    <% data.reviews.forEach(function(review) { %>
        <div>
            <p>Name: <%= review.reviewerName %></p>
            <p>Helpful</p>
            <p>Summary</p>
            <p>reviewText</p>
        </div>
    <% }); %>
    
</body>
</html>